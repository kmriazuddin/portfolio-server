export interface ISkill {
  name: string;
  images: string;
  skill: string;
  skillType: string;
}

export type ISkillUpdate = {
  name: string;
  images: string;
  skill: string;
  skillType: string;
};
